# Altair 插件系统详细分析报告

## 目录
1. [概述](#1-概述)
2. [核心数据结构](#2-核心数据结构)
3. [主题：从注册到生效的完整链路](#3-主题从注册到生效的完整链路)
4. [数据转换器：从注册到生效的完整链路](#4-数据转换器从注册到生效的完整链路)
5. [渲染器：从注册到生效的完整链路](#5-渲染器从注册到生效的完整链路)
6. [同类插件：并行启用的覆盖规则](#6-同类插件并行启用的覆盖规则)
7. [跨类插件：关系与冲突处理](#7-跨类插件关系与冲突处理)
8. [关键代码位置速查](#8-关键代码位置速查)

---

## 1. 概述

Altair 的插件系统包含三种独立的插件类型，它们在不同阶段参与图表的生成和显示：

| 插件类型 | 注册表类 | 作用阶段 | 核心职责 |
|---------|---------|---------|---------|
| **主题 (Themes)** | `ThemeRegistry` | 配置合并阶段 | 提供默认样式配置 |
| **数据转换器 (Data Transformers)** | `DataTransformerRegistry` | 数据处理阶段 | 转换数据格式 |
| **渲染器 (Renderers)** | `RendererRegistry` | 显示输出阶段 | 控制输出格式和方式 |

这三类插件共享相同的 `PluginRegistry` 基类，但各自独立运作，互不干扰。

---

## 2. 核心数据结构

### 2.1 PluginRegistry 基类

**位置**: `altair/utils/plugin_registry.py:83`

```python
class PluginRegistry(Generic[PluginT, R]):
    # 核心属性
    entry_point_group: str              # 入口点组名称，用于自动发现
    plugin_type: IsPlugin               # 类型检查函数
    _active: Plugin[R] | None           # 当前活跃插件实例
    _active_name: str                    # 当前活跃插件名称
    _plugins: dict[str, PluginT]        # 已注册插件字典 {名称: 插件}
    _options: dict[str, Any]             # 当前插件的选项
    _global_settings: dict[str, Any]     # 全局设置
```

### 2.2 状态管理

每个注册表维护完整的状态，支持保存和恢复：

```python
def _get_state(self) -> dict[str, Any]:
    return {
        "_active": self._active,
        "_active_name": self._active_name,
        "_plugins": self._plugins.copy(),
        "_options": self._options.copy(),
        "_global_settings": self._global_settings.copy(),
    }
```

这是上下文管理器能够临时切换并自动恢复的基础。

---

## 3. 主题：从注册到生效的完整链路

### 3.1 链路总览

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   注册阶段   │ ──▶ │   启用阶段   │ ──▶ │   获取阶段   │ ──▶ │   合并阶段   │ ──▶ │   应用阶段   │
│  register() │     │  enable()   │     │    get()    │     │update_nested│     │  渲染/显示  │
└─────────────┘     └─────────────┘     └─────────────┘     └─────────────┘     └─────────────┘
```

### 3.2 阶段一：注册

主题可以通过两种方式注册：

#### 方式一：显式注册

**位置**: `altair/vegalite/v6/theme.py:102-122`

```python
# 内置主题注册
themes.register(
    "default",
    lambda: {"config": {"view": {"continuousWidth": 300, "continuousHeight": 300}}},
)

themes.register(
    "opaque",
    lambda: {
        "config": {
            "background": "white",
            "view": {"continuousWidth": 300, "continuousHeight": 300},
        }
    },
)

# Vega 主题注册（通过 VegaTheme 类）
for theme in VEGA_THEMES:
    themes.register(theme, VegaTheme(theme))
```

#### 方式二：装饰器注册（用户自定义主题）

**位置**: `altair/theme.py:175-244`

```python
@theme.register("my_custom_theme", enable=True)
def custom_theme() -> theme.ThemeConfig:
    return {
        "autosize": {"contains": "content", "resize": True},
        "background": "#F3F2F1",
        "config": {
            "axisX": {"labelFontSize": 12, "titleFontSize": 12},
            "axisY": {"labelFontSize": 12, "titleFontSize": 12},
            "font": "'Lato', 'Segoe UI', Tahoma, Verdana, sans-serif",
        },
    }
```

装饰器内部实现：
```python
def decorate(func: Plugin[ThemeConfig], /) -> Plugin[ThemeConfig]:
    _register(name, func)           # 第一步：注册
    if enable:
        _themes.enable(name)         # 第二步：如果 enable=True，自动启用
    return func
```

#### 方式三：入口点自动发现

通过 `entry_point_group = "altair.vegalite.v6.theme"` 自动发现外部包中注册的主题。

### 3.3 阶段二：启用

启用操作将指定主题标记为"活跃"状态。

**位置**: `altair/utils/plugin_registry.py:205-224`

```python
def _enable(self, name: str, **options) -> None:
    # 步骤 1: 如果插件未注册，尝试从入口点加载
    if name not in self._plugins:
        try:
            (ep,) = (
                ep
                for ep in importlib_metadata_get(self.entry_point_group)
                if ep.name == name
            )
        except ValueError as err:
            raise NoSuchEntryPoint(self.entry_point_group, name) from err
        value = cast("PluginT", ep.load())
        self.register(name, value)
    
    # 步骤 2: 设置当前活跃插件（直接覆盖，无警告）
    self._active_name = name
    self._active = self._plugins[name]
    
    # 步骤 3: 处理选项
    for key in set(options.keys()) & set(self._global_settings.keys()):
        self._global_settings[key] = options.pop(key)
    self._options = options
```

**关键观察**：
- `_active_name` 和 `_active` 是**直接赋值**，没有检查或合并逻辑
- 这意味着**后启用的主题直接覆盖前一个**

#### 启用方式

```python
# 方式一：全局启用（永久，直到再次 enable）
alt.theme.enable("dark")

# 方式二：上下文管理器（临时，退出时自动恢复）
with alt.theme.enable("quartz"):
    # 此代码块内使用 quartz 主题
    chart.display()
# 退出后恢复之前的主题
```

### 3.4 阶段三：获取

**位置**: `altair/utils/plugin_registry.py:262-276`

```python
def get(self) -> partial[R] | Plugin[R] | None:
    if (func := self._active) and self.plugin_type(func):
        # 如果有选项，使用 partial 包装
        return partial(func, **self._options) if self._options else func
    elif self._active is not None:
        raise TypeError(f"活跃插件类型不正确")
    # ...
```

`get()` 返回的是一个**可调用对象**（函数或 partial），调用它才能获得实际的主题配置。

### 3.5 阶段四：合并（核心！）

这是主题系统**最重要**的阶段，决定了主题配置如何与图表配置合并。

**位置**: `altair/vegalite/v6/api.py:2124-2125`

```python
if func := theme.get():
    vegalite_spec = utils.update_nested(func(), vegalite_spec, copy=True)
```

**关键代码解读**：

1. `func := theme.get()`: 获取当前活跃主题函数
2. `func()`: 调用主题函数，获得**主题配置字典**
3. `utils.update_nested(主题配置, 图表配置, copy=True)`: 执行合并

#### update_nested 函数详解

**位置**: `altair/utils/core.py:807-849`

```python
def update_nested(
    original: Any,           # 第一个参数：被更新的对象
    update: Mapping[Any, Any],  # 第二个参数：用来更新的对象
    copy: bool = False,
) -> MutableMapping[Any, Any]:
    """
    嵌套更新字典
    
    注意参数顺序！
    - original: 原始值（会被 update 覆盖）
    - update: 更新值（优先级更高）
    """
    if copy:
        original = deepcopy(original)
    
    for key, val in update.items():
        if isinstance(val, Mapping):
            orig_val = original.get(key, {})
            if isinstance(orig_val, MutableMapping):
                # 递归合并子字典
                original[key] = update_nested(orig_val, val)
            else:
                # 非字典类型，直接覆盖
                original[key] = val
        else:
            # 叶子节点，直接覆盖
            original[key] = val
    
    return original
```

#### 合并优先级规则

```
update_nested(主题配置, 图表配置, copy=True)
              ↑              ↑
            original        update
              ↓              ↓
           低优先级        高优先级
```

**结论**：**图表配置会覆盖主题配置中的相同键**！

#### 合并示例

假设：

```python
# 主题配置（低优先级）
theme_config = {
    "config": {
        "view": {"continuousWidth": 300, "continuousHeight": 300},
        "axis": {"labelFontSize": 10}
    },
    "background": "white"
}

# 图表配置（高优先级）
chart_config = {
    "config": {
        "view": {"continuousWidth": 500},  # 与主题冲突
        "mark": {"color": "red"}            # 主题中没有
    },
    "encoding": {"x": ..., "y": ...}
}
```

合并结果：

```python
{
    "config": {
        "view": {"continuousWidth": 500, "continuousHeight": 300},  # width 被图表覆盖，height 保留主题
        "axis": {"labelFontSize": 10},                              # 来自主题
        "mark": {"color": "red"}                                    # 来自图表
    },
    "background": "white",            # 来自主题
    "encoding": {"x": ..., "y": ...}  # 来自图表
}
```

#### 特殊情况：usermeta 中的 embedOptions

VegaTheme 类通过 `usermeta` 传递 vega-embed 选项：

**位置**: `altair/vegalite/v6/theme.py:80-93`

```python
class VegaTheme:
    """Implementation of a builtin vega theme."""

    def __init__(self, theme: str) -> None:
        self.theme = theme

    def __call__(self) -> ThemeConfig:
        return {
            "usermeta": {"embedOptions": {"theme": self.theme}},  # 传给 vega-embed
            "config": {"view": {"continuousWidth": 300, "continuousHeight": 300}},
        }
```

这部分配置不会被 `update_nested` 特殊处理，遵循相同的合并规则。

### 3.6 阶段五：应用

合并后的配置最终通过以下方式应用：

1. **直接输出**: `chart.to_dict()` 返回合并后的完整 spec
2. **JSON 输出**: `chart.to_json()` 序列化为 JSON
3. **显示输出**: 当图表在 Jupyter 中显示时，渲染器使用合并后的 spec

---

## 4. 数据转换器：从注册到生效的完整链路

### 4.1 链路总览

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   注册阶段   │ ──▶ │   启用阶段   │ ──▶ │   获取阶段   │ ──▶ │   转换阶段   │
│  register() │     │  enable()   │     │    get()    │     │ _prepare_data│
└─────────────┘     └─────────────┘     └─────────────┘     └─────────────┘
```

### 4.2 阶段一：注册

**位置**: `altair/vegalite/v6/data.py:23-29`

```python
data_transformers = DataTransformerRegistry(
    entry_point_group="altair.vegalite.v6.data_transformer"
)

data_transformers.register("default", default_data_transformer)
data_transformers.register("json", to_json)
data_transformers.register("csv", to_csv)
data_transformers.register("vegafusion", vegafusion_data_transformer)
data_transformers.enable("default")
```

### 4.3 阶段四：转换（生效点）

数据转换器在 `_prepare_data` 函数中被调用：

**位置**: `altair/vegalite/v6/api.py:266-295`

```python
def _prepare_data(
    data: ChartDataType, context: dict[str, Any] | None = None
) -> ChartDataType | NamedData | InlineData | UrlData | Any:
    """
    将输入数据转换为 schema 可用的格式
    """
    # ... 类型检查 ...
    
    # 关键：调用数据转换器
    elif not isinstance(data, dict) and _is_data_type(data):
        if func := data_transformers.get():
            # 调用当前活跃的数据转换器
            data = func(nw.to_native(data, pass_through=True))
    
    # ... 后续处理 ...
```

**调用时机**：在 `to_dict()` 方法中处理数据时

**位置**: `altair/vegalite/v6/api.py:2100-2109`

```python
copy = _top_schema_base(self).copy(deep=False)
original_data = getattr(copy, "data", Undefined)
if not utils.is_undefined(original_data):
    # ... 数据类型处理 ...
    copy.data = _prepare_data(data, context)  # 调用数据转换器
    context["data"] = data
```

### 4.4 内置数据转换器说明

| 转换器 | 功能 | 输出格式 |
|-------|------|---------|
| `default` | 转换为内联值 | `{"values": [...]}` |
| `json` | 保存为 JSON 文件 | `{"url": "data.json", "format": {"type": "json"}}` |
| `csv` | 保存为 CSV 文件 | `{"url": "data.csv", "format": {"type": "csv"}}` |
| `vegafusion` | 使用 VegaFusion 处理大数据 | 特殊处理，支持千万行级数据 |

---

## 5. 渲染器：从注册到生效的完整链路

### 5.1 链路总览

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   注册阶段   │ ──▶ │   启用阶段   │ ──▶ │   获取阶段   │ ──▶ │   渲染阶段   │
│  register() │     │  enable()   │     │    get()    │     │_repr_mimebundle_│
└─────────────┘     └─────────────┘     └─────────────┘     └─────────────┘
```

### 5.2 阶段一：注册

**位置**: `altair/vegalite/v6/display.py:150-166`

```python
renderers = RendererRegistry(entry_point_group="altair.vegalite.v6.renderer")

renderers.register("default", html_renderer)
renderers.register("html", html_renderer)
renderers.register("colab", html_renderer)
renderers.register("kaggle", html_renderer)
renderers.register("zeppelin", html_renderer)
renderers.register("mimetype", mimetype_renderer)
renderers.register("jupyterlab", mimetype_renderer)
renderers.register("nteract", mimetype_renderer)
renderers.register("json", json_renderer)
renderers.register("png", png_renderer)
renderers.register("svg", svg_renderer)
renderers.register("jupyter", jupyter_renderer)
renderers.register("browser", browser_renderer)
renderers.register("olli", olli_renderer)

renderers.enable("default")
```

### 5.3 阶段四：渲染（生效点）

渲染器在 `_repr_mimebundle_` 方法中被调用，这是 Jupyter 显示对象的标准协议。

**位置**: `altair/utils/display.py:158-167`

```python
def _repr_mimebundle_(
    self, include: Any = None, exclude: Any = None
) -> MimeBundleType:
    """返回用于 Jupyter 前端显示的 MIME 包"""
    if self.renderers is not None:
        renderer_func = self.renderers.get()  # 获取当前活跃渲染器
        assert renderer_func is not None
        return renderer_func(self.spec)        # 调用渲染器
    else:
        return {}
```

### 5.4 内置渲染器说明

| 渲染器 | 输出 MIME 类型 | 适用场景 |
|-------|---------------|---------|
| `html` / `default` | `text/html` | Jupyter Notebook, Colab, Kaggle |
| `mimetype` | `application/vnd.vegalite.v6.json` | JupyterLab, nteract |
| `json` | `application/json` | 调试，查看原始 spec |
| `png` | `image/png` | 导出为图片 |
| `svg` | `image/svg+xml` | 导出为矢量图 |
| `jupyter` | 特殊 (JupyterChart) | 交互式控件 |
| `browser` | 打开浏览器 | 在浏览器中查看 |
| `olli` | `text/html` (Olli 模板) | 无障碍图表 |

---

## 6. 同类插件：并行启用的覆盖规则

### 6.1 核心原则：单一活跃

Altair 插件系统的**核心设计原则**是：

> **同一类型的插件在任意时刻只能有一个处于活跃状态**

这通过以下数据结构保证：
```python
_active: Plugin[R] | None   # 单一实例，不是列表
_active_name: str            # 单一名称
```

### 6.2 规则一：后启用覆盖先启用

当多次调用 `enable()` 时，后调用的直接覆盖先调用的。

**位置**: `altair/utils/plugin_registry.py:220-221`

```python
self._active_name = name    # 直接赋值，无检查
self._active = self._plugins[name]
```

**示例**：

```python
alt.renderers.enable("html")   # _active_name = "html"
alt.renderers.enable("svg")    # _active_name = "svg" (覆盖 html)
alt.renderers.enable("png")    # _active_name = "png" (覆盖 svg)

print(alt.renderers.active)    # "png"
```

**没有警告，没有合并，直接覆盖。**

### 6.3 规则二：显式注册覆盖入口点

当存在同名插件时，显式注册的优先级高于入口点自动发现的。

**位置**: `altair/utils/plugin_registry.py:206-219`

```python
def _enable(self, name: str, **options) -> None:
    # 关键检查：只有 _plugins 中不存在时，才去查入口点
    if name not in self._plugins:
        try:
            (ep,) = (ep for ep in importlib_metadata_get(self.entry_point_group) 
                     if ep.name == name)
        except ValueError:
            raise NoSuchEntryPoint(...)
        value = ep.load()
        self.register(name, value)  # 加载后注册到 _plugins
    # ...
```

**场景**：
1. 外部包 `altair-extra-themes` 通过入口点注册了 `"fancy"` 主题
2. 用户在代码中显式注册了同名主题：
   ```python
   alt.themes.register("fancy", my_custom_theme)
   ```
3. 此时 `_plugins["fancy"]` 存储的是用户显式注册的版本
4. 调用 `enable("fancy")` 时，由于 `name in self._plugins` 为 `True`，**跳过入口点加载**
5. 使用用户的版本

### 6.4 规则三：上下文管理器临时覆盖

上下文管理器内的启用会临时覆盖全局状态，退出时自动恢复。

**实现原理**: `PluginEnabler` 类

**位置**: `altair/utils/plugin_registry.py:52-80`

```python
class PluginEnabler(Generic[PluginT, R]):
    def __init__(
        self, registry: PluginRegistry[PluginT, R], name: str, **options: Any
    ) -> None:
        self.registry = registry
        self.name = name
        self.options = options
        # 步骤 1: 保存当前完整状态
        self.original_state: dict[str, Any] = registry._get_state()
        # 步骤 2: 启用新插件
        self.registry._enable(name, **options)
    
    def __enter__(self) -> PluginEnabler[PluginT, R]:
        return self
    
    def __exit__(self, typ: type, value: Exception, traceback: TracebackType) -> None:
        # 步骤 3: 恢复原始状态
        self.registry._set_state(self.original_state)
```

**嵌套示例**：

```python
alt.theme.enable("default")           # 全局状态: default

with alt.theme.enable("dark"):
    print(alt.theme.active)           # "dark" (临时覆盖)
    
    with alt.theme.enable("quartz"):
        print(alt.theme.active)       # "quartz" (嵌套覆盖)
    
    print(alt.theme.active)           # "dark" (恢复)

print(alt.theme.active)               # "default" (恢复)
```

**状态保存内容**：
```python
def _get_state(self) -> dict[str, Any]:
    return {
        "_active": self._active,
        "_active_name": self._active_name,
        "_plugins": self._plugins.copy(),
        "_options": self._options.copy(),
        "_global_settings": self._global_settings.copy(),
    }
```

**每个 `PluginEnabler` 保存完整的独立状态，嵌套时形成状态栈。**

### 6.5 规则四：同名注册直接覆盖

当使用相同名称注册多个插件时，后注册的直接覆盖先注册的。

**位置**: `altair/utils/plugin_registry.py:147-173`

```python
def register(self, name: str, value: PluginT | None) -> PluginT | None:
    if value is None:
        return self._plugins.pop(name, None)  # 注销
    elif self.plugin_type(value):
        self._plugins[name] = value            # 字典赋值，天然覆盖
        return value
    else:
        raise TypeError(...)
```

**示例**：

```python
def theme_v1():
    return {"config": {"view": {"width": 300}}}

def theme_v2():
    return {"config": {"view": {"width": 500}}}

alt.themes.register("my_theme", theme_v1)
alt.themes.register("my_theme", theme_v2)  # 直接覆盖 v1

# 此时 _plugins["my_theme"] = theme_v2
```

**没有警告，没有版本管理，直接覆盖。**

### 6.6 同类插件优先级总结表

| 场景 | 优先级 | 说明 |
|------|--------|------|
| 多次 `enable()` | 后调用 > 先调用 | 直接赋值覆盖 |
| 显式注册 vs 入口点 | 显式注册 > 入口点 | `_plugins` 优先检查 |
| 上下文嵌套 | 内层 > 外层 | 每层保存独立状态 |
| 上下文 vs 全局 | 上下文 > 全局 | 临时覆盖，退出恢复 |
| 多次 `register()` 同名 | 后注册 > 先注册 | 字典赋值覆盖 |

---

## 7. 跨类插件：关系与冲突处理

### 7.1 三类插件的独立性

**核心结论**：主题、数据转换器、渲染器是**完全独立**的三类插件，互不干扰。

#### 独立性证据

1. **独立的注册表实例**：
   ```python
   # altair/vegalite/v6/theme.py
   themes = ThemeRegistry(entry_point_group="altair.vegalite.v6.theme")
   
   # altair/vegalite/v6/data.py
   data_transformers = DataTransformerRegistry(entry_point_group="altair.vegalite.v6.data_transformer")
   
   # altair/vegalite/v6/display.py
   renderers = RendererRegistry(entry_point_group="altair.vegalite.v6.renderer")
   ```

2. **独立的启用状态**：
   ```python
   alt.theme.active           # 独立
   alt.data_transformers.active  # 独立
   alt.renderers.active        # 独立
   ```

3. **独立的调用时机**：

| 插件类型 | 调用时机 | 触发方法 |
|---------|---------|---------|
| 数据转换器 | `to_dict()` 早期处理数据时 | `_prepare_data()` |
| 主题 | `to_dict()` 后期合并配置时 | `update_nested()` |
| 渲染器 | 显示输出时 | `_repr_mimebundle_()` |

### 7.2 在图表生命周期中的协作

虽然三类插件独立运作，但它们在图表的完整生命周期中**按顺序协作**：

```
用户创建图表
      │
      ▼
┌─────────────────┐
│  chart.to_dict()│
└────────┬────────┘
         │
         ▼
┌─────────────────┐     ┌─────────────────┐
│  数据处理阶段    │ ──▶ │ 数据转换器生效  │
│ _prepare_data() │     │  (可选，如 vegafusion) │
└─────────────────┘     └─────────────────┘
         │
         ▼
┌─────────────────┐     ┌─────────────────┐
│  配置合并阶段    │ ──▶ │   主题生效      │
│  update_nested  │     │  合并默认配置   │
└─────────────────┘     └─────────────────┘
         │
         ▼
┌─────────────────┐
│  返回 vegalite  │
│    spec 字典    │
└────────┬────────┘
         │
         ▼  (当图表需要显示时)
┌─────────────────┐     ┌─────────────────┐
│  显示输出阶段    │ ──▶ │   渲染器生效     │
│ _repr_mimebundle│     │  生成输出格式   │
└─────────────────┘     └─────────────────┘
```

### 7.3 跨类插件：不存在冲突

由于三类插件：
1. **管理不同**：独立的注册表
2. **职责不同**：处理数据、提供样式、控制输出
3. **时机不同**：按顺序在不同阶段调用
4. **状态独立**：启用状态互不影响

**结论**：**跨类插件之间不存在冲突问题**。

### 7.4 场景示例

```python
# 场景：同时启用不同类型的插件

# 启用主题
alt.theme.enable("dark")

# 启用数据转换器
alt.data_transformers.enable("vegafusion")

# 启用渲染器
alt.renderers.enable("svg")

# 三者独立运作，互不干扰
print(alt.theme.active)           # "dark"
print(alt.data_transformers.active)  # "vegafusion"
print(alt.renderers.active)        # "svg"

# 创建图表时，三者按顺序生效
chart = alt.Chart(df).mark_point().encode(x='x', y='y')
chart.display()
# 执行顺序：
# 1. 数据转换器处理 df (如果是 vegafusion)
# 2. 主题配置合并到 spec
# 3. 渲染器将 spec 转换为 SVG 输出
```

### 7.5 唯一的"交互"：数据转换器与 Vega 格式

**唯一的特殊情况**：当使用 `vegafusion` 数据转换器时，它会影响 `to_dict()` 的 `format` 参数。

**位置**: `altair/vegalite/v6/api.py:2137-2149`

```python
if context.get("pre_transform", True) and _using_vegafusion():
    if format == "vega-lite":
        msg = (
            'When the "vegafusion" data transformer is enabled, the \n'
            "to_dict() and to_json() chart methods must be called with "
            'format="vega". \n'
        )
        raise ValueError(msg)
    else:
        return _compile_with_vegafusion(vegalite_spec)
```

**说明**：
- 这**不是**插件之间的冲突
- 这是 `vegafusion` 的特性：它预评估数据转换，输出 Vega 格式而非 Vega-Lite
- 这是**功能限制**，不是**冲突**

---

## 8. 关键代码位置速查

### 8.1 核心基类

| 功能 | 文件路径 | 行号 |
|------|---------|------|
| `PluginRegistry` 基类定义 | `altair/utils/plugin_registry.py` | 83 |
| `_enable` 方法（启用逻辑） | `altair/utils/plugin_registry.py` | 205 |
| `get` 方法（获取活跃插件） | `altair/utils/plugin_registry.py` | 262 |
| `register` 方法（注册逻辑） | `altair/utils/plugin_registry.py` | 147 |
| `PluginEnabler` 上下文管理器 | `altair/utils/plugin_registry.py` | 52 |
| `_get_state` / `_set_state` | `altair/utils/plugin_registry.py` | 183, 193 |

### 8.2 主题相关

| 功能 | 文件路径 | 行号 |
|------|---------|------|
| `ThemeRegistry` 定义 | `altair/vegalite/v6/theme.py` | 31 |
| 内置主题注册 | `altair/vegalite/v6/theme.py` | 102-122 |
| `VegaTheme` 类 | `altair/vegalite/v6/theme.py` | 80 |
| 主题装饰器 `@register` | `altair/theme.py` | 175 |
| 主题合并（关键！） | `altair/vegalite/v6/api.py` | 2124-2125 |
| `update_nested` 函数 | `altair/utils/core.py` | 807 |

### 8.3 数据转换器相关

| 功能 | 文件路径 | 行号 |
|------|---------|------|
| `DataTransformerRegistry` 定义 | `altair/utils/data.py` | 94 |
| 数据转换器注册 | `altair/vegalite/v6/data.py` | 23-29 |
| `_prepare_data` 函数 | `altair/vegalite/v6/api.py` | 266 |
| 数据转换器调用点 | `altair/vegalite/v6/api.py` | 287-288 |

### 8.4 渲染器相关

| 功能 | 文件路径 | 行号 |
|------|---------|------|
| `RendererRegistry` 定义 | `altair/utils/display.py` | 37 |
| 渲染器注册 | `altair/vegalite/v6/display.py` | 150-166 |
| `Displayable` 基类 | `altair/utils/display.py` | 124 |
| `_repr_mimebundle_` 方法 | `altair/utils/display.py` | 158 |
| 渲染器调用点 | `altair/utils/display.py` | 163-165 |

---

## 附录：优先级规则速查表

### 同类插件优先级

```
┌─────────────────────────────────────────────────────────────┐
│                      同类插件优先级                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  多次 enable():  后调用 > 先调用  (直接覆盖)                │
│                                                             │
│  注册方式:      显式注册 > 入口点 (显式优先)                │
│                                                             │
│  上下文嵌套:    内层 > 外层  (状态栈)                       │
│                                                             │
│  上下文 vs 全局: 上下文 > 全局 (临时覆盖)                   │
│                                                             │
│  多次 register(): 后注册 > 先注册 (字典覆盖)                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 主题合并优先级

```
┌─────────────────────────────────────────────────────────────┐
│                    主题合并优先级                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  update_nested(主题配置, 图表配置, copy=True)               │
│              ↑              ↑                               │
│            original        update                           │
│              ↓              ↓                               │
│           低优先级        高优先级                           │
│                                                             │
│  结论: 图表配置会覆盖主题配置中的相同键！                     │
│                                                             │
│  示例:                                                       │
│    主题:  {"config": {"view": {"width": 300, "height": 300}}} │
│    图表:  {"config": {"view": {"width": 500}}}                 │
│    结果:  {"config": {"view": {"width": 500, "height": 300}}} │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 跨类插件关系

```
┌─────────────────────────────────────────────────────────────┐
│                    跨类插件关系                               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  主题、数据转换器、渲染器是完全独立的三类插件！               │
│                                                             │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐   │
│  │   主题      │    │ 数据转换器   │    │   渲染器    │   │
│  │ ThemeRegistry│   │DataTransformer│  │RendererRegistry│ │
│  └──────┬──────┘    └──────┬──────┘    └──────┬──────┘   │
│         │                  │                  │            │
│         │   独立运作        │   独立运作        │   独立运作   │
│         │   互不干扰        │   互不干扰        │   互不干扰   │
│         ▼                  ▼                  ▼            │
│  ┌─────────────────────────────────────────────────────┐  │
│  │              不存在跨类冲突问题！                      │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
│  协作时机 (按顺序):                                          │
│    1. 数据转换器: to_dict() 早期处理数据                    │
│    2. 主题:     to_dict() 后期合并配置                      │
│    3. 渲染器:   显示时 _repr_mimebundle_()                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```
